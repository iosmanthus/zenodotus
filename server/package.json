{
  "name": "@zenodotus/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/stdio.ts --bundle --platform=node --format=esm --outfile=dist/stdio.mjs --banner:js='#!/usr/bin/env node'"
  },
  "devDependencies": {
    "@zenodotus/api-spec": "workspace:*",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
